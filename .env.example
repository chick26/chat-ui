# Environment Variables Template for Multi-Scenario Chat UI
# Copy this file to .env and fill in your actual values
# The .env file will be ignored by git for security

# ==================== API Configuration ====================

# Chat API Base URL (without trailing slash)
REACT_APP_API_BASE_URL=http://your-api-server-ip

# API Version (defaults to v1)
REACT_APP_API_VERSION=v1

# ==================== Scenario-Specific API Tokens ====================

# Default API Token (fallback for all scenarios)
REACT_APP_API_TOKEN=your-default-api-token

# Route Planning Scenario Token
REACT_APP_API_TOKEN_ROUTE=your-route-api-token

# Cable Fault Scenario Token  
REACT_APP_API_TOKEN_CABLE=your-cable-api-token

# Maintenance Scenario Token
REACT_APP_API_TOKEN_MAINTENANCE=your-maintenance-api-token

# ==================== Instructions ====================
# 1. Copy this file: cp env.template .env
# 2. Edit .env with your actual API URLs and tokens
# 3. The .env file is already in .gitignore so it won't be committed
#
# Token Priority:
# - Each scenario will first try its specific token (e.g., REACT_APP_API_TOKEN_ROUTE)
# - If not found, falls back to REACT_APP_API_TOKEN
# - This allows you to use the same token for all scenarios or different tokens
#
# Scenarios:
# - route: Route planning and navigation queries
# - cable: Cable fault diagnosis and troubleshooting  
# - maintenance: Equipment maintenance and operational procedures
